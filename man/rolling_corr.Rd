% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastroll.R
\name{rolling_corr}
\alias{rolling_corr}
\title{Rolling Pearson correlation}
\usage{
rolling_corr(
  x,
  y,
  window,
  min_periods = window,
  return_cov = FALSE,
  skip_nan = FALSE
)
}
\arguments{
\item{x, y}{numeric vectors of equal length.}

\item{window}{window length, a positive integer.}

\item{min_periods}{minimum number of valid pairs required; defaults to
\code{window}.}

\item{return_cov}{if \code{TRUE}, also return the sample covariance.}

\item{skip_nan}{if \code{TRUE}, pairs with \code{NA} in either series are
excluded from the window instead of propagating.}
}
\value{
If \code{return_cov} is \code{FALSE}, a numeric vector of
  correlations; otherwise a list with elements \code{corr} and \code{cov}.
}
\description{
Computes the Pearson correlation between two series over a
  sliding window via O(1) running-sum updates.  A window in which either
  series is constant yields \code{NA} (never \code{Inf}).
}
\examples{
x <- rnorm(100)
rolling_corr(x, 2 * x + rnorm(100), window = 20)

}
\seealso{
\code{\link{rolling_cov}}
}
