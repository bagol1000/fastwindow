% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastroll.R
\name{rolling_multiple_regression}
\alias{rolling_multiple_regression}
\title{Rolling multiple linear regression}
\usage{
rolling_multiple_regression(y, X, window, min_periods = window)
}
\arguments{
\item{y}{numeric vector (dependent variable).}

\item{X}{numeric matrix with \code{length(y)} rows and \code{k} columns
(\code{1 <= k <= 16}); do not include an intercept column.}

\item{window}{window length, a positive integer.}

\item{min_periods}{minimum number of observations required; defaults to
\code{window}.}
}
\value{
A list with elements \code{coef} (matrix \code{n x (k+1)},
  intercept first), \code{r2} and \code{residual_std} (numeric vectors).
}
\description{
Ordinary least squares of \code{y} on \code{k} regressor
  columns over a sliding window.  The inverse Gram matrix is maintained
  incrementally via Sherman-Morrison rank-1 updates (O(k^2) per step)
  with a Cholesky fallback on near-singular steps.  An intercept is
  added internally: pass \code{k} regressors, receive \code{k + 1}
  coefficients with the intercept first.
}
\examples{
X <- matrix(rnorm(300), ncol = 3)
y <- X \%*\% c(1, 2, 3) + rnorm(100, sd = 0.1)
fit <- rolling_multiple_regression(as.numeric(y), X, window = 30)

}
\seealso{
\code{\link{rolling_regression_xy}}
}
