% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastroll.R
\name{rolling_regression_xy}
\alias{rolling_regression_xy}
\title{Rolling simple linear regression on an explicit regressor}
\usage{
rolling_regression_xy(y, x, window, min_periods = window)
}
\arguments{
\item{y}{numeric vector (dependent variable).}

\item{x}{numeric vector (regressor), same length as \code{y}.}

\item{window}{window length, a positive integer.}

\item{min_periods}{minimum number of observations required; defaults to
\code{window}.}
}
\value{
A list with elements \code{intercept}, \code{slope} and \code{r2},
  each a numeric vector of the same length as \code{y}.
}
\description{
Ordinary least squares of \code{y} on \code{x} over a
  sliding window, updated in O(1) per step via running sums.
}
\examples{
x <- rnorm(100)
y <- 2 * x + rnorm(100, sd = 0.1)
fit <- rolling_regression_xy(y, x, window = 20)

}
\seealso{
\code{\link{rolling_regression}}
}
