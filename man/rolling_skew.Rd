% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastroll.R
\name{rolling_skew}
\alias{rolling_skew}
\title{Rolling skewness}
\usage{
rolling_skew(x, window, min_periods = window, skip_nan = FALSE)
}
\arguments{
\item{x}{numeric vector.}

\item{window}{window length, a positive integer.}

\item{min_periods}{minimum number of valid observations required to
emit a value.  Defaults to \code{window}.}

\item{skip_nan}{if \code{TRUE}, missing values are excluded from the
window instead of propagating to the output.}
}
\value{
A numeric vector of the same length as \code{x}.
}
\description{
Bias-corrected rolling skewness (the convention of
  \code{pandas .rolling().skew()} and
  \code{scipy.stats.skew(bias = FALSE)}).  Requires at least 3 valid
  observations; zero-variance windows give \code{NA}.  Computed from
  shifted power sums re-anchored to the window mean every 4096 steps,
  so large mean offsets do not lose precision.
}
\examples{
rolling_skew(rnorm(100), window = 20)

}
\seealso{
\code{\link{rolling_kurt}}, \code{\link{rolling_zscore}}
}
