% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastroll.R
\name{rolling_zscore}
\alias{rolling_zscore}
\title{Rolling z-score}
\usage{
rolling_zscore(
  x,
  window,
  min_periods = window,
  ddof = 1L,
  skip_nan = FALSE,
  n_threads = 0L
)
}
\arguments{
\item{x}{numeric vector.}

\item{window}{window length, a positive integer.}

\item{min_periods}{minimum number of valid observations required to
emit a value.  Defaults to \code{window}.}

\item{ddof}{\code{1L} for the sample standard deviation (n-1
denominator, default), \code{0L} for population.}

\item{skip_nan}{if \code{TRUE}, missing values are excluded from the
window instead of propagating to the output.}

\item{n_threads}{OpenMP threads forwarded to the mean/std kernels
(AVX2 builds only); \code{0L} (default) means single-threaded.}
}
\value{
A numeric vector of the same length as \code{x}.
}
\description{
\code{(x - rolling mean) / rolling sd}, with \code{NA}
  where the input is missing, the window does not emit (see
  \code{min_periods}), or the window standard deviation is zero.
}
\examples{
rolling_zscore(rnorm(100), window = 20)

}
\seealso{
\code{\link{rolling_mean}}, \code{\link{rolling_std}}
}
